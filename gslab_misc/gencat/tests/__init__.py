'''
This directory contains the `gencat` library's unit tests. These tests can be run using
`python -m unittest discover`
from `gencat/` or from `gencat/tests/`. To run the tests with logging, use:
`python run_all_tests.py`
from `gencat/tests/`.
'''
