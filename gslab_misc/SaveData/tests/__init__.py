'''
This directory contains the `SaveData` library's unit tests. These tests can be run using
`python -m unittest discover`
from `SaveData/` or from `SaveData/tests/`. To run the tests with logging, use:
`python run_all_tests.py`
from `SaveData/tests/`.
'''
