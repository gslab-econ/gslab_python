DESCRIPTION
==========================================================
Creates an abstract class for classical minimum distance estimation (CMD). Three inputs are required for CMD estimation: statistics, weight matrix, and a function. Subclasses must specify a function that converts parameters into the relevant statistics. Implements a CMD subclass for estimating the mean and sd of a random normal variable as an example.

NOTES
==========================================================